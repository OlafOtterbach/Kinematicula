name: build and test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        lfs: true
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore ./Kinematicula/Kinematicula.csproj
    - name: Build
      run: dotnet build ./Kinematicula/Kinematicula.csproj --no-restore --configuration Release
    - name: Test
      run: dotnet test ./Kinematicula/Kinematicula.sln --no-build --verbosity normal
